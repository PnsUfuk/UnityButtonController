using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Include
{
    public enum PockState { Free, Owned };
    public enum PlayerState { Free,HasPock };

    public enum ButtonState {non , Down , Up , Press};
}